{
  "name": "eduscheduler",
  "private": true,
  "packageManager": "npm@11.9.0",
  "workspaces": [
    "docs"
  ],
  "scripts": {
    "build": "npm --prefix docs run build",
    "build:client": "npm --prefix docs run build:client",
    "dev": "npm --prefix docs run dev",
    "start": "npm --prefix docs run start",
    "test": "npm --prefix docs run test"
  }
}
